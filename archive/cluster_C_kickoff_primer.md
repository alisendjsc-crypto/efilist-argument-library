# Cluster C kickoff — paste to a fresh Library chat (Opus MAX required)

**Class:** MAX (philosophy-dense archetype-variant authoring + cold-grading). Confirm effort before spending tool budget.
**Scope:** ONE declared deliverable = cluster C carriers. Two red-button variant blocks:
- (a) **sophisticate / NU-entailment** — the objection that antinatalism collapses into / is entailed by negative utilitarianism.
- (b) **defender / repugnance** — the "this view is monstrous/repugnant" register.

## Do FIRST (verify-before; file tree is ground truth)
1. Open `v3_9_archetype_triage_PROVISIONAL.json` → confirm the exact `node_id` each red-button maps to (do NOT fabricate ids). Coverage-check both in corpus `efilist_argument_library_v3_8_0.json` via `grep -n`/targeted `json.load` — never full-view (~1.3MB).
2. Re-issue tree-verified `verify_before` facts against **post-K64 tips** (K64 render sweep lands a v3.9.1 pin + canon 37.24, render-only, zero corpus/data mutation — your carriers are unaffected). If your authoring mount diverges from the re-issued facts, **record BOTH values** (K62 carriers did this; it adjudicated cleanly).

## Authoring
- **One file per node** (`{node}_sophisticateVariant_v3_9.json`, `{node}_defenderVariant_v3_9.json`) — never a batched multi-node build (output-cutoff risk).
- **Asymmetry-decoupling spine:** NU-entailment touches suffering-focus — deploy Benatar's asymmetry as the spine even if suffering-abolition is granted arguendo. Don't stake the rebuttal on persistence-of-suffering.
- Honest concessions route to sibling nodes; no overreach.

## Carrier schema — keep K62's EXACTLY (pattern off `cherry_picking_worst_sophisticateVariant_v3_9.json`)
Top keys: `artifact`(=node_archetypeVariants_block), `node_id`, `built_in_session`, `scaffold_ref`, `staging_status`(SIDE-CAR), `checkpoint_gating`, `intra_node_route_closed`, `parent_refutation_summary`, `inherited_grade`{policy,source_ledger_node,verification_read,axes,short,medium,long,headline_grade_long,inheritance_label}, `responses_archetypeVariants`, `slots_not_built`, `_variant_meta`{variant_axis,grade_policy,build_status,distinctness_note}, `slot_attestation`, `route_target_justification`, `guard_attestation`.
- **ONE addition (relay item 5):** embed the **full md5** of every base register you verified against (corpus, combined.html, index, jsx, canon, ledger) in the verification block. K62 used byte-count only; the relay-spec wants md5 — close that drop.

## Grading
- Cold-grade: **freeze the axes tuple before consulting any reference score**; disclose contamination explicitly if it occurs.
- RSI = (v·s·c·r·a)^(1/5)·100; bands A>=88 / B[82,88) / C[76,82) / D<76 inclusive. **Score-targeting prohibited** — prose improves on its own; grades are downstream.

## Close
- Carriers are SIDE-CAR side-cars → Cowork folds to all four surfaces (you do not splice mega-literal files inline).
- Emit carriers + `session_..._state.json` to outputs; present_files; no postamble.
- Ledger band-correction (prior session) and any C-node grade entries are separate — do not re-touch the corrected ledger here.
