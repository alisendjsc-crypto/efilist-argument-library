{
  "artifact": "k352_functional_control_v0_1",
  "generated_by": "tools/k352_functional_gate.py",
  "subject": "combined.html -- the level-independent affordance (K352)",
  "method": "real Chromium via Playwright over file://, all http(s) requests aborted; the pinned base run as a NULL for every deep-link claim",
  "base_md5": "72187f6cf0fccdf8e9f4ec6ca5ce009c",
  "candidate_md5": "f095c0ce0e5a1d796d57fa5a5dd62f7d",
  "node_under_test": "care-ethics",
  "checks": [
    "ok   candidate boots with no uncaught page error",
    "ok   candidate renders all 82 cards",
    "ok   candidate default depth is medium (NOT flipped)",
    "ok   [DISMANTLE] renders on every card at the default level",
    "ok   ...and reports 'not at depth' there",
    "ok   COPY LINK carries NO suffix at the default level",
    "ok   activation sets responseLevel=long",
    "ok   activation syncs the global depth control (setDepth path, not a parallel one)",
    "ok   activation lands the reader ON the card",
    "ok   the affordance now reports 'at depth' on every card",
    "ok   COPY LINK now carries @long on every card",
    "ok   the affordance still renders at long (it is level-INDEPENDENT)",
    "ok   NULL: on the BASE, #obj-<id> lands the reader at MEDIUM -- the broken link",
    "ok   NULL: the base has no affordance at all",
    "ok   #obj-<id>@long boots straight to long",
    "ok   #obj-<id>@long focuses the card",
    "ok   #obj-<id>@long raises no page error",
    "ok   #obj-<id>@short boots to short (the grammar is general, not a long-only hack)",
    "ok   APPEND-ONLY: bare #obj-<id> still lands at medium and still focuses, exactly as K108",
    "ok   an unknown depth token does NOT match the carve-out (falls to the legacy promote)",
    "ok   a bare anchor does NOT reset a reader who is already at long",
    "ok   an unknown id with a valid depth degrades silently, no error, no depth change",
    "ok   APPEND-ONLY: #rwe-<id> still promotes to #/rwe/instance:<id>",
    "ok   #rwe-<id>@long is NOT claimed -- it falls through to the legacy promote, as before",
    "ok   live hashchange (page already booted) also forces the depth",
    "ok   note layer UNCHANGED: 9 note divs, 7 revealers -- the coupling is queued, not repaired",
    "ok   ccclxxvii control: without the reset the witness SURVIVES, so the guard can fail",
    "ok   ccclxxvii control: with the reset it does not"
  ],
  "n_checks": 28,
  "failures": [],
  "verdict": "GREEN"
}
